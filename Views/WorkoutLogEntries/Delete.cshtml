@model WellnessTracker.Models.WorkoutLogEntry

@{
    ViewData["Title"] = "Delete Workout Entry";
}

<h1>Delete Workout Entry</h1>

<h3>Are you sure you want to delete this entry?</h3>

<div>
    <h4>Workout Entry Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Date)</dt>
        <dd class="col-sm-10">@Model.Date.ToString("MMMM dd, yyyy")</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.ExerciseName)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.ExerciseName)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Duration)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.Duration) minutes</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.CaloriesBurned)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.CaloriesBurned) kcal</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.Notes)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.Notes)</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger me-2" />
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
